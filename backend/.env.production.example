# Production Environment Variables
# Copy this file to .env for production deployment

PORT=3000
NODE_ENV=production

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database_name

# Clerk Authentication (Production Keys)
CLERK_SECRET_KEY=sk_live_your_production_secret_key
CLERK_PUBLISHABLE_KEY=pk_live_your_production_publishable_key

# Security Settings
# All development bypasses are disabled in production
# Users must authenticate through Clerk
# Roles are managed through Clerk public metadata

# Optional: Allowed Origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://yourdomain.com,https://bartender.yourdomain.com