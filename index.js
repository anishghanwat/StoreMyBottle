// This is a root-level entry point for Railway deployment
// The actual backend code is in the /backend directory

console.log('StoreMyBottle - Please deploy from /backend directory');
console.log('This root file is just for Railway detection');

// Redirect to backend if someone tries to run this
process.exit(1);